// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// 新規・読込・保存・エクスポート・インポート・Undo・Redo（4.4, 4.8）。
// 画面右上、SVGアイコンのみ+ホバーヒント表示で配置する（6.3）。

const name = 'Toolbar';

const props = {
  // TODO: canUndo, canRedo
};

const emits = [
  // TODO: 'new-sheet', 'open-sheet-list', 'save', 'export', 'import', 'undo', 'redo'
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
