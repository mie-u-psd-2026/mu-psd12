// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// AI提案の表示とコミット/却下（4.3）。画面中央下部に配置する。
// 提案は常にトランザクション単位で一括承認/却下する。

const name = 'AiProposalPanel';

const props = {
  // TODO: proposal（title, ops, ghosts, removes, links, group, note, merge）
};

const emits = [
  // TODO: 'commit', 'reject'
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
