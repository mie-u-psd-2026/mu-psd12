// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// AIモデル選択（上方向に開くプルダウン）とシステムプロンプト入力（4.2）。
// 画面右下に常設で配置する（6.2）。

const name = 'ModelSelect';

const props = {
  // TODO: models（GET /models の結果）, currentModel, systemPrompt
};

const emits = [
  // TODO: 'model-changed', 'system-prompt-changed'
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
