// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// シートタイトルのインライン編集（4.8）。画面右上に配置する（6.2）。

const name = 'SheetTitle';

const props = {
  // TODO: title
};

const emits = [
  // TODO: 'title-changed'
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
