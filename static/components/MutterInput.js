// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// ひとりごとメモ（4.7）。画面左下に常設で配置する。
// 入力内容をAIに読ませ、シートへの反映提案（AiProposalPanel）につなげる。

const name = 'MutterInput';

const props = {};

const emits = [
  // TODO: 'submit'（入力確定時。POST /ai 相当の提案生成につなげる）
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
