// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// 保存済みシート一覧（タイトル・更新日時）の表示と選択読込、新規作成（4.8）。
// 明示的な境界を持つパネルとして表示してよい（6.2の例外）。

const name = 'SheetListPanel';

const props = {
  // TODO: sheets（GET /sheets の結果: [{id, title, updated_at}]）
};

const emits = [
  // TODO: 'select', 'create-new', 'close'
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
