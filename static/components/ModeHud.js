// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// 現在の操作モード表示（4.1）。画面左上、アプリ名の下に配置する（6.2）。

const name = 'ModeHud';

const props = {
  // TODO: mode（'view' | 'add' | 'remove' | 'edit' | 'join' | 'group'）
};

const emits = [];

function setup(props) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
