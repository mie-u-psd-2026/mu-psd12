// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// join/groupモードの簡易インライン編集パネル（4.1）。
// 間接エッジのコメント編集・削除、グループのタイトル・コメント編集・削除・新規作成に共用する。
// ネイティブダイアログ（prompt/confirm）は使用しない。

const name = 'EditPanel';

const props = {
  // TODO: hasTitle, title, comment, x, y
};

const emits = [
  // TODO: 'save'（title, comment）, 'delete'
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
