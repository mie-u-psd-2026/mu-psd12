// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// 汎用ラジアルメニュー（モードホイール / AIホイール / グループホイールで共用）。
// 各項目はアイコン+ホバー時のヒント表示のみとし、境界は持たせない（6.2, 6.3）。

const name = 'RadialWheel';

const props = {
  // TODO: entries（[{icon, label, value}]）, center（x, y）, activeIndex
};

const emits = [
  // TODO: 'select'
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
