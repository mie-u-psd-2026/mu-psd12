// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// 1ノードの表示・インライン編集（4.1 edit）。
// AI提案中（未コミット）の場合は破線枠+点滅で強調表示する（6.1）。

const name = 'SheetNode';

const props = {
  // TODO: node（id, kind, text, x, y）, isGhost, isGhostRemove
};

const emits = [
  // TODO: 'text-committed' 等
];

function setup(props, { emit }) {
  // TODO: インライン編集（contenteditable、確定操作で反映。4.1）
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
