// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// グループの半透明領域とタイトル・コメント表示（4.1, 6.1）。
// groupモード中、タイトルのクリックで EditPanel を開く（タイトル・コメント編集、削除）。

const name = 'SheetGroup';

const props = {
  // TODO: group（id, members, title, comment, color）
};

const emits = [
  // TODO: 'edit-requested'（groupモード中のタイトルクリック時）
];

function setup(props, { emit }) {
  return {};
}

const template = ``;

export default { name, props, emits, setup, template };
