// ============================================================
// プレースホルダーファイル。
// 実装時には、このコメントを含む全てのプレースホルダーコメントを削除すること。
// ============================================================
// シートの変更履歴 Undo/Redo（4.4）。
// - 直近20トランザクションまで保持する
// - 1トランザクション（AI一括変更 or ユーザー1操作）を1履歴単位とする
// - シートの保存/読込を跨いで保持しない（読込時にリセットする）

export function useUndoRedo() {
  // TODO: snapshot() — 変更前状態をpastへ積む（上限20、futureはクリア）
  // TODO: undo() / redo()
  // TODO: reset() — シート読込時に呼び出す
  return {};
}
