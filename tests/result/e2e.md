# E2E自動テスト結果

対象: [tests/e2e/run.mjs](../e2e/run.mjs)（Playwright、実`app.py`に対する実ブラウザ操作。AI非依存の定型操作のみ）

| 日時 | コマンド | 結果 |
|------|----------|------|
| 2026-09-09 | `docker run --rm -v "<repoルート>/tests/e2e:/app" -w /app -e BASE_URL=http://host.docker.internal:5000 node:24-slim sh -c "npm install && npx playwright install --with-deps chromium && npm test"` | 9件PASS、2件SKIP |
| 2026-09-09 | 同上 | サブツリー削除（600ms化）・カーソル・ビネット・マウスホイール表示・右クリック即時表示・テーマノード拡大・未保存確認ダイアログを追加。実装が追いつくまで**1件目（サブツリー削除）でFAILし停止する想定**（意図的なred状態）。個別に検証した結果、新規4ブロック（カーソル・ビネット・マウスホイール・右クリック即時表示）は想定通り赤、テーマ拡大・確認ダイアログは既存実装のまま緑、後続の既存項目も正常に完走することを確認済み |
| 2026-09-09 | 同上 | BE/FE実装完了後、**17件中17件全PASS、SKIP 0件**。保存バグ解消により「保存・読込一覧反映・再読込」「シート削除（確認付き）」もSKIPから解消 |

## 既知バグ（解消済み）

`PUT /sheet/{id}`のAPI契約不一致（BE側が`{title, body:{...}}`のネストを要求していた）は解消済み。詳細はWBS.mdの結合テスト履歴を参照。
