# テスト仕様書: brAIn Storming

仕様の詳細は [design-document.md](design-document.md) を参照する。テスト結果は本書に書かず [tests/result/](tests/result/) に記録する。

## 1. テスト対象

- `app.py` / `services/`（Flaskバックエンド）
- `static/`（Vue.jsフロントエンドUI）

## 2. テスト環境

- Windows 10/11
- Python 3.13、Flask、openai SDK（`.venv` 仮想環境。構築手順は [readme.md](readme.md) 参照）
- ローカルOllamaが起動済み、モデル導入済み（例: `qwen2.5-coder:0.5b`。readme.md参照）
- ブラウザ: Chrome（シナリオを代表実施）。Edge・Firefoxは表示崩れがないかの目視確認のみ

## 3. 自動テスト

テストコードは全て [tests/](tests/) 配下に置く（`tests/backend`, `tests/frontend`, `tests/e2e`）。手動シナリオ（4章以降）は自動テストが対象外の範囲（実ブラウザ操作・実LLM応答など）のみを扱う。

### 3.1 バックエンド（[tests/backend/test_backend.py](tests/backend/test_backend.py)）

実行: `.venv\Scripts\activate` の後 `python -m unittest tests.backend.test_backend`。結果は [tests/result/backend.md](tests/result/backend.md) に記録する。

| No | 項目 | 期待結果 |
|----|------|----------|
| 1 | `SheetFormatServiceTest` | シートのLLM向けシリアライズ・区切り文字エスケープ・LLM応答のパース・エクスポート/インポート形式の相互変換が仕様通り動作する |
| 2 | `DatabaseServiceTest` | シートのCRUD・一覧取得・`state`のマージ更新が仕様通り動作する |
| 3 | `ApiErrorFormatTest` | 共通エラー形式（`validation`/`not_found`/`llm_unavailable`/`llm_timeout`/`internal_error`）、シート作成・取得・更新・削除、`POST /ai`成功/異常系の応答が仕様通り |

### 3.2 フロントエンド（[tests/frontend/run.mjs](tests/frontend/run.mjs)）

実行方法・前提は [tests/frontend/README.md](tests/frontend/README.md) を参照（jsdom上でVueを動かし、バックエンド応答はモック）。結果は [tests/result/frontend.md](tests/result/frontend.md) に記録する。

| No | 項目 | 期待結果 |
|----|------|----------|
| 1 | 設定・モデル一覧の起動時復元、全コンポーネントのマウント | 仕様通り動作する |
| 2 | 600ms長押し・取消、サブツリー削除、テーマ保護、Undo | 仕様通り動作する（design-document.md 4.1） |
| 3 | 接続作成・コメント編集・削除、逆向き重複防止 | 仕様通り動作する |
| 4 | グループ作成・所属・除外・名前/コメント/色編集 | 仕様通り動作する |
| 5 | ノートCRUD、改行・テキスト表示、Undo | 仕様通り動作する |
| 6 | 物理配置・重なり解消・長文サイズ・収束停止・根固定 | 仕様通り動作する |
| 7 | AI前自動保存、差分表示、却下・一括承認、実ID変換、全体Undo/Redo | 仕様通り動作する |
| 8 | AI不正提案の全体拒否、統合での子・接続・所属整理、既存ノード編集 | 仕様通り動作する |
| 9 | 保存失敗時AI中止、非同期処理中の編集ロック、古い提案の拒否 | 仕様通り動作する |
| 10 | JSON入出力・未保存確認・新規ID・履歴上限 | 仕様通り動作する |
| 11 | グループ単一所属・空グループ削除・Undo | 仕様通り動作する |
| 12 | 設定保存・メモ送信・AIホイール・全体/部分の対象選択 | 仕様通り動作する |
| 13 | 設定の通信競合防止 | 仕様通り動作する |
| 14 | 保存・読込・最後のシートID更新・確認付きシート削除 | 仕様通り動作する |
| 15 | 保存成功でUndo/Redoリセット、保存失敗で保持、AI承認を新規履歴化 | 仕様通り動作する |
| 16 | 前回シートの自動復元と履歴リセット | 仕様通り動作する |
| 17 | API応答不正・ネットワーク障害・不正インポートのエラーハンドリング（`GET /models`/`GET /state`/`GET /sheets`/`GET /sheet`の不正形式、5MB超過・壊れたJSONのインポート） | 仕様通り動作する |
| 18 | モード別カーソル表示（view=grab、他=crosshair） | 仕様通り動作する（design-document.md 6.4） |
| 19 | 画面全体のビネット効果の表示 | 仕様通り動作する（design-document.md 6.2） |
| 20 | マウスホイール操作でのモードホイール表示 | 仕様通り動作する（design-document.md 4.1） |
| 21 | 右クリック長押しからモードホイール表示までがほぼ即時であること | 仕様通り動作する（design-document.md 4.1） |
| 22 | グループ内引力・グループ間斥力による物理配置 | 仕様通り動作する（design-document.md 6.4） |

### 3.3 E2E（[tests/e2e/run.mjs](tests/e2e/run.mjs)、Playwright・AI非依存の定型操作）

3.2はAPI応答をモックしたjsdom上の検証だが、こちらは実際に起動中の`app.py`に対して実ブラウザ（Chromium）から操作し、フロントエンド〜バックエンドの結合を確認する。AIモード（`/ai`呼び出し）を伴う手順は対象外（4章で手動確認）。

実行方法・前提は [tests/e2e/README.md](tests/e2e/README.md) を参照（`node`イメージの一時Dockerコンテナで実行）。結果・既知バグは [tests/result/e2e.md](tests/result/e2e.md) に記録する。

| No | 項目 | 期待結果 |
|----|------|----------|
| 1 | 起動時のシート表示、タイトル編集とUndo/Redo | 仕様通り動作する |
| 2 | ノード追加（子・孫） | 仕様通り動作する |
| 3 | ノード削除（600ms長押し・短い長押しでは削除しないこと・子孫ノードもサブツリーごと削除・Undo） | 仕様通り動作する（design-document.md 4.1） |
| 4 | ノード接続（作成・コメント編集・削除・Undo） | 仕様通り動作する |
| 5 | グループ作成・タイトル編集・除外・Undo | 仕様通り動作する |
| 6 | ノートCRUD・Undo | 仕様通り動作する |
| 7 | 保存・読込一覧反映・再読込 | 仕様通り動作する |
| 8 | エクスポート・インポートの往復整合性 | 仕様通り動作する |
| 9 | シート削除（確認付き） | 仕様通り動作する |
| 10 | 異常系: テーマノード削除の保護 | 仕様通り動作する |
| 11 | モード別カーソル表示（view=grab、他=crosshair） | 仕様通り動作する（design-document.md 6.4） |
| 12 | 画面全体のビネット効果の表示 | 仕様通り動作する（design-document.md 6.2） |
| 13 | マウスホイール操作での実wheelイベントによるモードホイール表示 | 仕様通り動作する（design-document.md 4.1） |
| 14 | 右クリック長押しから実時間でのモードホイール即時表示 | 仕様通り動作する（design-document.md 4.1） |
| 15 | メインテーマノードの拡大表示（同一テキストでのboundingBox比較） | 仕様通り動作する（design-document.md 6.4） |
| 16 | 未保存の変更がある状態での確認ダイアログ表示（「新規」操作時） | 仕様通り動作する（design-document.md 4.8） |

## 4. 手動シナリオテスト（AI関連）

編集・履歴・保存/読込・エクスポート/インポート・異常系などAIの絡まない定型操作は3.3のE2Eで自動化済みのため、ここでは自動化していないAI関連の手順のみを対象にする。

### 4.1 シナリオ1: AIによるシート編集（4.2, 4.3, 4.4）

| No | 操作 | 期待結果 |
|----|------|----------|
| 1 | ノードを選択し、右クリック長押し中にShiftでAIショートカットホイールを表示して「深掘り」を選択 | 画面中央下部にAI提案パネルが表示され、対象がシート上でハイライトされる |
| 2 | 提案を却下 | シートに変更が反映されない |
| 3 | 再度AI機能を実行し、提案を承認 | シートに変更が反映され、Undo履歴が1件追加される |
| 4 | 画面右上のUndoアイコンをクリック | AI承認による変更が取り消される |

### 4.2 シナリオ2: ひとりごとメモ・AIノート作成（4.6, 4.7）

| No | 操作 | 期待結果 |
|----|------|----------|
| 1 | 画面左下のひとりごと入力欄にアイデアを入力し送信 | AIがノード/ノート追加を提案するパネルが表示される |
| 2 | 提案を承認 | 新しいノードまたはノートがシートに追加される |
| 3 | 右クリック長押し+ShiftのAIショートカットホイールから「ノートに要約」を選択し、対象をシート全体にする | シート全体を要約したノート案が提案される |
| 4 | 提案を承認 | ノートが成果物として追加される |

### 4.3 シナリオ3: AI異常系

| No | 操作 | 期待結果 |
|----|------|----------|
| 1 | シートを保存する前（未保存の状態）でAI機能を実行しようとする | フロントが呼び出し直前に自動保存してからAI要求を送ること、保存失敗時はAI要求が中止されることを確認する（[tests/frontend/run.mjs](tests/frontend/run.mjs)の「保存失敗時AI中止」で自動確認済みだが、実ブラウザでの見え方を確認） |
| 2 | AIの提案中（承認/却下前）に他の編集や保存を試みる | 操作がロックされ実行できないことを画面上で確認できる |

## 5. 実Ollama接続確認（任意・参考）

自動テスト・上記シナリオはAPI応答をモック/決定的な操作のみで確認しているため、実際のOllama応答の品質やタイムアウト系は別途、時間があれば確認する。必須項目ではない。

| No | 内容 | 手順 | 期待結果 |
|----|------|------|----------|
| 1 | 実AI応答の妥当性 | 深掘り・新視点・関連付け・統合・グループ化・ノート生成の各AIモードを実際にOllamaで実行 | 各モードの指示内容（design-document.md 8.2）に沿った応答が返る |
| 2 | Ollama未起動時のエラー | Ollamaを停止した状態でAI機能を実行 | 画面にエラーが表示される（`llm_unavailable`, 503） |
| 3 | LLMタイムアウト | 応答が120秒以上返らない状況を再現してAI機能を実行 | 画面にタイムアウトのエラーが表示される（`llm_timeout`, 504） |

## 6. 非機能要件

自動テストでカバー済みの項目（Undo/Redo履歴の上限・変更履歴の非永続化・エクスポート/インポートの往復整合性）は削除した（[tests/frontend/run.mjs](tests/frontend/run.mjs) 項目10・15・16、[tests/e2e/run.mjs](tests/e2e/run.mjs) 項目8参照）。

| No | 内容 | 手順 | 期待結果 |
|----|------|------|----------|
| 1 | 大量ノード時の物理演算負荷 | 30〜50個程度のノードを持つシートを開く | 物理シミュレーションによる自動配置が収束し、ブラウザが応答不能にならない |
