# AIサポート付きブレストアプリ 仕様書

## 1. 概要

作成するシステム: 「**AIサポート付きブレストアプリ**」  

AIのサポート機能がついたブレインストーミングのシート管理アプリである。  
利用者はこれを利用することで、**ブレインストーミングを効率的に進める**ことができる。  

## 2. スコープ定義

納期と品質を担保するため、以下の通りにスコープを制限する。  

- **1クライアントによるオンライン利用**に限定
  - 複数人で利用する場合も、単一クライアントで利用することになる
- LLMは**ローカルモデル**を利用する
- 利用者の選択するローカルモデルに依存するため、**利用モデルによる性能差は保証しない**
  - 推論精度
  - 推論速度
  - 消費リソース etc.
- ブレインストーミングのシートは以下の仕様に限定する
  - 「**ノード**」「**エッジ**」「**グループ**」から構成される
  - メインテーマノードを最上位として、**樹状に階層的に広がるノード群**
  - ノードは単語や短い文章を表す
  - ノード同士はエッジによりつながり、1つのノードは以下のエッジを持つ
    - 1個の上位ノードへつながる直接エッジ
    - 0個以上の別ノードへつながる間接エッジ
    - つまり、孤立ノードは存在せず、**直接エッジによる構造はメインテーマノードを根とする木構造**となる
  - グループは複数のノードの集合を表す
    - 意味的に関連の深いノードの集合
    - グループには、タイトルとコメントが付する

> 「**ノート**」はメインの木構造とは独立したデータ構造である。  
> 1つのノートは「タイトル」「本文」からなり、シートとは異なり、文章の表現に適する。  

## 3. 要求整理  

このシステムを利用する利用者が求めること  

- ブレインストーミングのシートをGUIで編集できる
- AIが新しい視点を発見・提案する
- AIが全体・一部のアイデアを要約する
- AIがシートを整理（ノードを繋げる・作る・消す）する
- AIがシートを破壊しない

## 4. 機能要件

要求を達成するために必要な機能  

### 4.1 シート編集機能

シートを構成する各要素のCRUD  

- ノード
- エッジ
- グループ
- ノート

> CRUD: Create/Read/Update/Delte。データの読み書きの一連の動作  

ユーザは以下の物理操作でシートを編集する。  

- 左クリック
  - 各モード（後述）に従う操作
- マウスホイール
  - モードの切り替え
- Ctrl + マウスホイール
  - ズームイン・アウト
- 右クリック長押し
  - モードホイールの表示
  - マウス操作 + 長押し解除 でモード切り替え
- 右クリック長押し中の Shift
  - AI機能ショートカットホイールの表示
  - マウス操作 + 長押し解除 でAI機能を発火

> マウス操作とトラックパッド操作の挙動の違いに対する対策が必要  

必要な操作モード  

- ビュー `view`
  - 左クリックのドラッグでシートを見回す
- ノード追加 `add`
  - 左クリックしたノードに子ノードを追加
- ノード削除 `remove`
  - カーソルを合わせたノードの削除
  - 一定時間左クリック長押しで削除
- ノード編集 `edit`
  - 左クリックしたノードのアイデアを編集

### 4.2 AIによる変更のコミット

AIが破壊的変更を加えた際、ユーザの想定してしない変更（ブレストシートの破綻）を防ぐための仕組み  

- AIが一連の変更手順（トランザクション）を作成し、ユーザに提案する
- ユーザが提案を許可することで、トランザクションが適用される（コミット）
- 提案が拒否された場合は、トランザクションは適用されない

### 4.3 シートの変更履歴（Undo, Redo）

4.2 に合わせて、さらにブレストシートの破綻を防ぐための手戻りの仕組み  

- 数手前まで変更を残してやり直しができるようにする

### 4.4 AIによるシートの編集

AIにトランザクションの提案を行わせる  

- 特定のノードから、直接ノードを生成（＝関連の深いノード）
- 特定のノードに限らず、ノードを生成（＝新しい視点のノード）
- 新しいエッジ（間接エッジ）の作成
- ノードの統合（複数の直接エッジ上のノードを1つのノードにまとめる）
- ノードのグルーピング

### 4.5 AIによるノートの作成

AIがシート全体、また一部を要約して生成した文章を、ノートとして成果物にする。

### 4.6 ひとりごとメモ

ユーザがひとりごとのように、浮かんだアイデアを文章として記述する。  
AIはそれを読み、シートに新しいノードやノートとして反映する提案をする。  

### 4.7 シートの保存/読込・エクスポート/インポート

- アプリ内でシートを保存/読込する
- シートを構造化データのファイルとしてエクスポート/インポートする

## 5. 技術スタック

フロントエンドではCDNライブラリを利用する。

- フロントエンド: javascript, HTML5/CSS3
  - Vue.js: フレームワーク
- バックエンド: python
  - Flask: フレームワーク
  - OpenAI: LLMモデルAPI
- DB
  - sqlite

## 6. デザインガイドライン

目的（ブレストシート）以外に対する、ユーザの認知負荷を下げる方針でデザインを設計する。  

### 6.1 カラーセット

アプリの基本カラーセットはモノクロ  

> 具体値未定  

AI関連機能のカラーセットはビビッド・サイバーカラー  

> 具体値未定  

ノードのグループのカラーセットはパステル  

> 具体値未定  

### 6.2 領域のマージン

画面領域は極力仮想的に分割し、意味なくボーダーやカード・ペイン表示などで明示的に分割しない（明示的な境界を持たせない）。  
その代わりに、マージン領域を十分に確保することで認知負荷を抑えて分割する。

ただし、ノードやホイールなど、明示的な領域分割に意味を持つオブジェクトに対しては境界を持たせることを許可する。  

## 7. コーディング規約

### 7.1 命名規則

- 変数名
  - キャメルケースで、意味のある命名（何を格納する変数なのかが分かる）を心がける。Ex. `inputData`
- 関数名
  - パスカルケース。Ex. `
- クラス名
  - パスカルケース
- 定数名
  - すべて大文字のスネークケース
- ファイル名
  - コンポーネントファイルはパスカルケース
  - それ以外は小文字のスネークケース
- Vueコンポーネント名
  - パスカルケース
- 真偽値（boolean）の命名（`is`, `has` 等のプレフィックス方針）
  - `is`: 状態の真偽（～であるか）。Ex. `isActive`
  - `can`: 権限の真偽（～が可能か）。Ex. `canEdit`
  - `has`: 所持の真偽（～を所持するか）。Ex. `hasError`
- 略語の扱い（許可する略語・禁止する略語）
  - 可読性に寄与しない略語は禁止する。Ex. `usr`, `a11y`
  - 一般的に用いられている範囲での、可読性を高めるための略語は許可する。Ex. `err`, `ctx`
  - ループ変数は `i` 以降のアルファベット1字を階層順に割り当てる。
  - 特定のブロック内のみなど、局所的に用いられる一時変数に対しては、アルファベット1字の命名を許可する。Ex. `ctx.onError(e => Logger(e))`
> 対象言語: JavaScript, Python  

### 7.2 関数の分割

- 1関数あたりの責務の粒度（単一責任の目安）
- 1関数あたりの行数・ネストの目安
- 引数の数の目安、多引数になる場合の対処（オブジェクト引数化など）
- 副作用の扱い（純粋関数を優先する範囲）
- 早期return可否と7.3ガード節との関係

### 7.3 ガード節

- ガード節を使う条件分岐の基準（else省略の方針）
- 異常系・例外系を先に弾く書き方の統一
- ネストを深くしないための基準（ネスト許容段数）
- バリデーション処理での適用例

### 7.4 コンポーネント分割（Vue.js）

- コンポーネント分割の粒度基準（Atomic Design等の採用可否）
- Props / Emit の設計方針
- ディレクトリ構成（例: `components/`, `views/`, `composables/` 等）
- 状態管理の方針（コンポーネントローカル vs 共有状態）
- Composition API の利用方針（`<script setup>` の採用等）
- スタイルのスコープ方針（`scoped` CSS 等）

## 8. 内部設計

### 8.1 API設計

### 8.2 DB設計
