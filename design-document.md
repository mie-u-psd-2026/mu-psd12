# AIサポート付きブレストアプリ 仕様書

## 1. 概要

AIのサポート機能がついたブレインストーミングのシート管理アプリである。  
利用者はこれを利用することで、**ブレインストーミングを効率的に進める**ことができる。  

## 2. スコープ定義

以下の通りにスコープを制限する。  

- **1クライアントによるオンライン利用**に限定
  - 複数人で利用する場合も、単一クライアントで利用することになる
  - 「オンライン利用」とは、フロントエンドのCDNライブラリ読み込みにインターネット接続を要することを指す（LLM推論自体はローカルのOllamaで完結する）
- LLMは**ローカルモデル（Ollama）**を利用する
  - 初期リリースではローカルモデルのみに限定し、オンラインモデル（クラウドAPI）への対応は将来拡張とする
  - 利用モデルはハードコードせず、利用可能なモデルの一覧を取得したうえでユーザーが選択・切替できるようにする
- **利用モデルによる性能差は保証しない**
  - 推論精度
  - 推論速度
  - 消費リソース等
- ブレインストーミングのシートは以下の仕様に限定する
  - 「**ノード**」「**エッジ**」「**グループ**」から構成される
  - メインテーマノードを最上位として、**樹状に階層的に広がるノード群**
  - ノードは単語や短い文章を表す
  - ノード同士はエッジによりつながり、1つのノードは以下のエッジを持つ
    - 1個の上位ノードへつながる直接エッジ
    - 0個以上の別ノードへつながる間接エッジ
    - つまり、孤立ノードは存在せず、**直接エッジによる構造はメインテーマノードを根とする木構造**となる
  - グループは複数のノードの集合を表す
    - 意味的に関連の深いノードの集合
    - グループには、タイトルとコメントが付する

> 「**ノート**」はメインの木構造とは独立したデータ構造である。  
> 1つのノートは「タイトル」「本文」からなり、シートとは異なり、文章の表現に適する。  

## 3. 要求整理  

このシステムを利用する利用者が求めること  

- ブレインストーミングのシートをGUIで編集できる
- AIが新しい視点を発見・提案する
- AIが全体・一部のアイデアを要約する
- AIがシートを整理（ノードを繋げる・作る・消す）する
- AIがシートを破壊しない

## 4. 機能要件

要求を達成するために必要な機能  

### 4.1 シート編集機能

シートを構成する各要素のCRUD（Create/Read/Update/Delete）  

- ノード
- エッジ
- グループ
- ノート

ユーザは以下の物理操作でシートを編集する。  

- 左クリック
  - 各モード（後述）に従う操作
- マウスホイール
  - モードの切り替え。操作中はモードホイールを表示し、選択中の項目を強調表示する
- Ctrl + マウスホイール
  - ズームイン・アウト
- 右クリック長押し
  - モードホイールの表示（長押しから表示までの遅延は体感できない程度、即座に表示する）
  - マウス操作 + 長押し解除 でモード切り替え
- 右クリック長押し中の Shift
  - AI機能ショートカットホイールの表示
  - マウス操作 + 長押し解除 でAI機能を発火（4.5 AIによるシートの編集、4.6 AIによるノートの作成が対象。4.7 ひとりごとメモは対象外。常設の入力欄で操作する）

<!-- TODO: マウス操作とトラックパッド操作の挙動の違いへの対応方針を定義する -->

マウスホイールによる切り替えと、右クリック長押しのモードホイールによる切り替えは併用する。いずれもモードホイールを表示する操作であり、一方の操作中にもう一方の操作が開始された場合は、即座に前者の入力を中断して後者に主導権を移す。これは連続して切り替わった場合も同様で、結果的には常に最後に操作していた入力が適用される。  

モードの並び順: `view → add → remove → edit → join → group`  

必要な操作モード  

- ビュー `view`
  - 左クリックのドラッグでシートを見回す
- ノード追加 `add`
  - 左クリックしたノードに子ノードを追加
- ノード削除 `remove`
  - カーソルを合わせたノードの削除
  - 左クリック長押し600msで削除
  - 削除対象に子孫ノードがある場合は、子孫ノードも全て共に削除する（サブツリーごと削除）
  - メインテーマノードは削除できない
- ノード編集 `edit`
  - 左クリックしたノードのアイデアをインライン編集する（ノード上で直接テキストを編集し、確定操作で反映する）
- ノード接続 `join`
  - 始点ノードを選択し、終点ノードを選ぶことで間接エッジを作成する
  - 初期実装では無向エッジ・単一の破線スタイルのみとする（有向エッジ対応、線の意味的サブタイプの選択、端点マークの編集は将来拡張とする）
  - 無向のため、既に同じ2ノード間に間接エッジが存在する場合、逆向きの接続は重複として作成できない
  - 間接エッジのコメントは必須項目とする。始点・終点ノードを選んだ直後、エッジ中点に入力欄がインライン表示され、コメントを確定するまではエッジ自体を作成しない（空欄のまま確定・取消した場合は何も作成されない）
  - 間接エッジのコメントはエッジ中点付近に常時表示する（ホバー時のみの表示はしない）
  - joinモード中は、既存の間接エッジをクリックするとコメントがその場でインライン編集状態になる（サイドパネルは使用しない）。編集中に削除もできる。空欄のまま確定・取消した場合は元のコメントを維持する（コメントを空にはできない）
- グループ化 `group`
  - ノードを選択するとグループホイールが表示され、既存グループへの追加、または新規グループの作成を選べる
  - 1ノードにつき1グループまでとする（複数グループへの同時所属は不可）
  - 既にグループに属するノードを選択すると、グループホイールに「除外」の選択肢が表示される
  - グループホイールの既存グループ項目は、グループカラーの丸アイコンで示し、ホバー時にグループ名をヒント表示する
  - groupモード中は、グループのタイトル・コメント・色を編集できる（グループの削除もこの編集画面から行う）。編集画面はアプリ内の簡易インラインパネルとし、ネイティブダイアログは使用しない

### 4.2 LLMを用いたAI機能の提供

- ローカルLLM（Ollama）によりAI機能を提供する
- インストールされているモデルの一覧から、利用するモデルを選択・切替できる
- AIへの指示に用いるプロンプト（システムプロンプト）を、利用者がカスタマイズできる

基本設定UIは画面右下に固め、モデル選択は上方向のドロップダウンメニュー、システムプロンプトはその下に領域を確保する。

### 4.3 AIによる変更のコミット

AIが破壊的変更を加えた際、ユーザの想定していない変更（ブレストシートの破綻）を防ぐための仕組み  

- AIが一連の変更手順（トランザクション）を作成し、ユーザに提案する
  - 変更対象のノード/エッジ/グループは、6.1で定義する強調表示（破線枠等）でシート上に直接ハイライトする
- ユーザが提案を許可することで、トランザクションが適用される（コミット）
- 提案が拒否された場合は、トランザクションは適用されない
- 提案は常にトランザクション単位で一括承認/却下する（部分承認は将来拡張とする）

UIは画面中央下部に表示する。

### 4.4 シートの変更履歴（Undo, Redo）

4.3 に合わせて、さらにブレストシートの破綻を防ぐための手戻りの仕組み  

- 直近20トランザクションまで変更を保持し、Undo/Redoできるようにする
- Undo/Redoの単位は1トランザクション（AIによる一括変更、ユーザーによる1操作のいずれもコミット単位で1履歴とする）
- 変更履歴はシートの保存/読込を跨いで保持しない（読込時にリセットする）

UIは画面右上に、SVGアイコンのみで配置する。ホバー時にアイコン下に項目名をヒント表示する。  

### 4.5 AIによるシートの編集

AIにトランザクションの提案を行わせる  

- 特定のノードから、直接ノードを生成（＝関連の深いノード）
- 特定のノードに限らず、ノードを生成（＝新しい視点のノード）
- 新しいエッジ（間接エッジ）の作成
- ノードの統合（複数の直接エッジ上のノードを1つのノードにまとめる）
- ノードのグルーピング

特定の要素の選択が必要な機能は、AI機能利用時にそれをGUI上でクリックすることで選択できる。  

### 4.6 AIによるノートの作成

AIがシート全体、また一部を要約して生成した文章を、ノートとして成果物にする。  
ユーザーも同じノートに対し、手動で作成・編集・削除できる。

### 4.7 ひとりごとメモ

ユーザがひとりごとのように、浮かんだアイデアを文章として記述する。  
AIはそれを読み、シートに新しいノードやノートとして反映する提案をする。  

UIは画面左下に配置する。  

### 4.8 シートの保存/読込・エクスポート/インポート

- アプリは複数のシートを保持できる（8.2の`GET /sheets`/`POST /sheet`に対応）
- 「読込」は、保存済みシート一覧（タイトル・更新日時）をパネルで表示し、選択したシートを読み込む
- 「新規」から、空のシートを新規作成できる
- 現在編集中のシートを保存する
- シートを構造化データのファイルとしてエクスポート/インポートする
  - ファイル形式はJSONに固定する
  - インポートは常に新規シートとして扱う（8.1.1参照）
- 未保存の変更がある状態で「新規」「読込」「インポート」を行う場合は、破棄の確認ダイアログを表示する
- 保存済みシートの削除時も、確認ダイアログを表示する
- 起動時に、最後に開いていたシート（`last_opened_sheet_id`）と設定（`last_used_model`, `user_prompt`）を自動的に復元する（8.3参照）

UIは画面右上に、SVGアイコンのみで配置する。ホバー時にアイコン下に項目名をヒント表示する。

シートタイトルは画面右上に表示し、インライン編集できるようにする。  

## 5. 技術スタック

フロントエンドではCDNライブラリを利用する。  
Vite/webpack等のビルドツールは利用せず、ビルドステップなしで完結させる（Vueコンポーネントも `.vue`（SFC）ではなく `.js` のコンポーネントオブジェクトとして記述する）。  
CDNで読み込む全てのライブラリ（Vue.js含む）は、特定バージョンに固定する。  

- フロントエンド: javascript, HTML5/CSS3
  - Vue.js: フレームワーク
  - Feather Icons: 6.3のアイコン表示に用いる（CDN、特定バージョンに固定）
- バックエンド: python
  - Flask: フレームワーク
  - OpenAI: LLMモデルAPI（ローカルのOllamaへ接続する。詳細は8.2参照）
- DB
  - sqlite

## 6. デザインガイドライン

目的（ブレストシート）以外に対する、ユーザの認知負荷を下げる方針でデザインを設計する。  

### 6.1 カラーセット

用途別に3種のカラーセットを用意する。具体的なHEX値は仮値とし、最終確定は別途デザインレビューで行う。  

- 基本（モノクロ）: グレースケール（`#1a1a1a` 〜 `#f5f5f5` 系）
- AI関連機能（ビビッド・サイバー）: ネオンシアン系（`#00e5ff` 等）
- ノードのグループ（パステル）: 低彩度パステル系（`#d4e4f7` 等）

エッジ・グループ・AI提案状態の視覚的な描き分けは以下の通り。  

- 直接エッジ: 実線
- 間接エッジ: 破線
- グループ: メンバーノードの背後に、グループカラーの半透明領域を敷く
- AI提案中（未コミット）の変更: 破線枠や点滅など、一時的な強調表示で区別する

### 6.2 領域のマージン

画面領域は極力仮想的に分割し、意味なくボーダーやカード・ペイン表示などで明示的に分割しない（明示的な境界を持たせない）。  
その代わりに、マージン領域を十分に確保することで認知負荷を抑えて分割する。

ただし、ノードなど、明示的な領域分割に意味を持つオブジェクトに対しては境界を持たせることを許可する。ショートカットホイール（モードホイール・AIホイール）の各項目はこの例外に含めず、他の常設UIと同様に境界を持たせない。  

常設UI要素（タイトル、モード表示、シートタイトル、ツールバー、ノート一覧、ひとりごとメモ入力欄、AIモデル選択プルダウン、システムプロンプト入力欄等）は、明示的な境界（ボーダーやカード）を持たせずマージンのみで配置する。  

- アプリ名: 画面左上
- モード表示: アプリ名の下
- シートタイトル: 画面右上（4.8参照）
- AIモデル選択プルダウン: 画面右下、上方向へ開く

ノードやグループに対する操作は、右クリック長押し等のホイール系操作で行う。  

- 対応画面サイズ: デスクトップ専用にスコープを制限する（タブレット/スマートフォン対応は行わない）
- 余白の基準グリッド単位: 8px
- フォント: 追加のCDN読み込みを行わず、OSのシステムフォントを使用する
- 画面全体の周辺部に薄いビネット効果（中心から外側にかけて白くフェードする効果）を敷き、コンテンツ領域へ視線を誘導する
- AIモデル選択・システムプロンプト欄（画面右下）の内部要素は左詰めで配置する
- ひとりごとメモ入力欄（画面左下）は、内容を書き込みやすいよう横幅を広めに確保する
- ひとりごとメモ・システムプロンプトの入力欄は背景を透明にし、プレースホルダー文字は通常のミュート色（`--mono-500`系）より一段階濃い色にして視認性を確保する

### 6.3 機能のアイコン表示

ショートカットホイールや各機能を提供するボタンは、原則機能を示すアイコンで示し、ホバー時に適当な位置に機能名をヒント表示する。

### 6.4 ノードの表示

座標はデータとして永続化せず、物理シミュレーションによって自動的にノードの重なりを解決する。
シミュレーターによる自動解決とアニメーションは、シートの読込時、ノード数が変化した際（追加・削除・AI提案のコミット等）、および間接エッジが生成・消滅した際に開始し、収束すれば停止する。

- 全ノード間に斥力（距離の逆二乗）を働かせ、重なりを防ぐ
- 直接エッジに引力、間接エッジにも弱い引力を働かせて、関連ノード同士が視覚的に近づく
- 同じグループに属するノード同士はより強く引き合い、異なるグループ（無所属を含む）に属するノード同士はより強く反発し合う
- メインテーマノードは固定座標で、あらゆる力の影響を受けない
- メインテーマノードは、通常のアイデアノードより一回り大きく表示する（枠線の太さだけでなく、サイズ自体を拡大する）

背景のドットグリッドは、パン・ズーム操作に追従して移動・拡縮する（キャンバスと一体の座標系として振る舞う）。  

マウスカーソルは操作モードに応じて変化させる。  

- `view`モード: 掴む形（グラブカーソル）。ドラッグ中は「掴んでいる」形に変える
- 上記以外のモード: 十字カーソル

## 7. コーディング規約

> 対象言語: JavaScript, Python  

### 7.1 命名規則

簡潔で、意味のある命名を心がける。  
複数単語を連結した命名は4単語以内に抑え、文章のような冗長な命名は行わない。  

> 何を格納する変数なのか、どういうプロセスを持つ関数なのか等が名前で分かる。  

- 変数名
  - キャメルケース。Ex. `inputData`
- 関数名
  - キャメルケース。Ex. `fetchSheetData()`
  - Vueハンドラ名（テンプレートの `@click` 等から呼ばれるメソッド）
    - `handle` プレフィックスをつけたキャメルケース。Ex. `handleClick()`
    - `on` プレフィックスは使用しない
- クラス名
  - パスカルケース。Ex. `Client`
- 定数名
  - すべて大文字のスネークケース。Ex. `STORAGE_DIR`
- ファイル名
  - Vueコンポーネントファイルはコンポーネント名。Ex. `PrimaryButton.js`
  - それ以外は小文字のスネークケース。Ex. `database_controller.py`
- Vueコンポーネント名
  - パスカルケース。Ex. `PrimaryButton`
- Vueコンポーネントのprops名
  - 定義（JS側）はキャメルケース。Ex. `props: { inputValue: String }`
  - テンプレート側で渡す際はケバブケース。Ex. `<primary-button input-value="...">`
- Vueカスタムイベント名（`emits`宣言・`emit()`呼び出し）
  - JS側（`emits`宣言・`emit()`の第1引数）はキャメルケース。Ex. `emits: ['itemSelected']` / `emit('itemSelected', payload)`
  - テンプレート側で受け取る際は、複数単語であれば必ずケバブケース。Ex. `<app-list @item-selected="onSelect">`
  - `on` プレフィックスはイベント名自体には付けない
- 真偽値（boolean）の命名（`is`, `has` 等のプレフィックス方針）
  - `is`: 状態の真偽（～であるか）。Ex. `isActive`
  - `can`: 権限の真偽（～が可能か）。Ex. `canEdit`
  - `has`: 所持の真偽（～を所持するか）。Ex. `hasError`
- 略語の扱い（許可する略語・禁止する略語）
  - 可読性に寄与しない略語は禁止する（知名度が低い、元の単語が短い、意味を持たないもの等）。Ex. `usr`, `a11y`, `btn`, （脈絡のない）`a`
  - 可読性を高める一般的な略語は許可する（元の単語が長い、知名度が高いもの等）。Ex. `err`, `ctx`
  - ループ変数は `i` 以降のアルファベット1字を階層順に割り当てる
  - 局所的に用いられる一時変数は、アルファベット1字の命名を許可する。Ex. `ctx.onError(e => Logger(e))`

### 7.2 関数の分割

処理を関数として切り出すかどうかは、再利用性と局所性で判断する。  

- プロセスのラッピング
  - 同モジュール内で使い回す可能性の高い処理はラッピングする。Ex. フォームの送信APIラッパー
  - 一度きりの局所性の高い処理はラッピングせず、直接記述する。Ex. クラスのインスタンス生成
- 投機的な関数分割の禁止
  - 現時点で使い回す予定がない処理を、将来使い回す可能性があるという理由だけで関数として切り出すことを禁止する
  - 使い回しが確定した時点で、あらためて切り出しを行う
- 関数宣言
  - 引数
    - 元がオブジェクトである引数は、メンバに分解せずオブジェクトそのものを受け取る
    - 引数をまとめるためだけのオブジェクト化は禁止する
  - ネスト
    - 関数内のネストは原則2階層まで
    - 3階層以降のネストは、可読性の向上が見込める場合にのみ関数切り出しを行う
- 副作用の扱い
  - 同一コンポーネント及び下位コンポーネントに対する副作用は許可する
- 早期return
  - 許可する。7.3のガード節と合わせて用いる
- コメント
  - コードをそのまま翻訳したようなコメントは不要
  - 関数宣言の直前に、「何を受け取り、どういうプロセスを行い、何を返すか」を簡潔に表した1行コメントを残す
  - 定数宣言には、その定数の役割を示すコメントを付す
  - 変数宣言にはコメント不要

### 7.3 ガード節

異常系・例外系を関数の先頭で弾き、正常系の処理をネストの深い位置に置かない。  

- バリデーションが必要な処理は関数の先頭で行い、最初に異常系を弾く
- ifのネストは原則1階層まで
- 7.2の早期returnと組み合わせて用いる

### 7.4 コンポーネント分割（Vue.js）

コンポーネントのモノリス化を防ぎ、状態管理の見通しを保つ方針で分割する。  

> `.vue`（SFC）ではなく `.js` のコンポーネントオブジェクトとして定義する。  
> `template` プロパティにテンプレート文字列を持たせ、`setup(props)` でComposition APIを用いる。  
> Ex. `export default { name: 'PrimaryButton', props: {...}, setup(props) { ... }, template: '<button>...</button>' }`  

- 原則、1コンポーネントファイルには1つのVueコンポーネントのみを記述する
- 既存コンポーネントに変更を加える際は、必ずコンポーネントの切り出しを検討する
- Atomic Designに従い、コンポーネントを粒度ごとに分割する（Ex. Atoms / Molecules / Organisms）
- 1つ以上のコンポーネントを跨って共有する状態はComposition API（Ex. `composables/`）を用いる
- ファイル内の構成順序
  - 依存関係のインポート → 定義 → エクスポートの順に記述する
  - 定義部分は `name` → `props` → `emits` → `setup` → `template` の順に並べる
  - オブジェクトリテラルと`export`を1文にまとめず、定義してから最後に `export default {name, props, emits, template, setup}` のようにまとめてエクスポートする
  - 参考実装: `static/components/AppButton.js`
- コメント
  - コンポーネントファイルの先頭に、コンポーネントの説明コメントを付す（3行以内。複数行可）

### 7.5 Python/Flask構成

- サービス層（DB操作・AIセッション処理等のビジネスロジック）は `services/` ディレクトリに分割する。Ex. `services/database_service.py`, `services/ai_session_service.py`
- ルーティング（Flaskの `@app.route`）は当面 `app.py` に集約し、Blueprintによる分割はルート数が増加した時点で検討する

## 8. 内部設計

### 8.1 データスキーマ

#### 8.1.1 シート

シート本体（ノード/エッジ/グループ/ノート）の構造は、内部保存形式・エクスポート/インポート形式のいずれも共通とする。両者の違いはメタデータの有無のみ。  

**内部保存形式**（`/storage/sheets/{uuid}.json`。8.3参照）: 本体のみを保存する。タイトル・作成/更新日時は含めない（`sheets`テーブル側で管理。8.3参照）。  

```
{
  "nodes": [
    { "id": "n0", "kind": "theme" | "idea", "text": "string", "parent": "nodeId | null" }
  ],
  "links": [
    { "id": "l1", "a": "nodeId", "b": "nodeId", "comment": "string" }
  ],
  "groups": [
    { "id": "g1", "members": ["nodeId", "..."], "title": "string", "comment": "string", "color": "string" }
  ],
  "notes": [
    { "id": "note1", "title": "string", "body": "string" }
  ]
}
```

**エクスポート/インポート形式**（4.8参照）: `sheets`テーブル側のメタデータを`metadata`として本体に同梱し、ファイル単体で自己完結させる。  

```
{
  "metadata": { "title": "string", "created_at": "string", "updated_at": "string" },
  "nodes": [ /* 内部保存形式と同じ */ ],
  "links": [ /* 内部保存形式と同じ */ ],
  "groups": [ /* 内部保存形式と同じ */ ],
  "notes": [ /* 内部保存形式と同じ */ ]
}
```

- インポート時は常に新規シートとして扱う（既存シートへの上書きはしない）。シートIDは元のファイルに含めず、インポート時に新規採番する
- `nodes[].parent`: `null`はメインテーマノード（根）を表す。直接エッジはこの`parent`参照によって表現され、独立したレコードを持たない
- `links[]`: 間接エッジ。1件が1つのjoinモードでの接続に対応する
- `groups[].color`: 6.1で定義するパステルカラーセットの値
- 各配列内の要素順序に意味は持たせない（`id`で参照する）

#### 8.1.2 LLM入出力

シートのシリアライズ・LLM応答のパースは、いずれもバックエンドに実装を集約する。以下の文法に従う。  

- 行指向・パイプ区切り記法
- 1レコード1行、フィールドは `|` 区切り
- ネスト・インデント・波括弧は使わず、1行が独立して`split('|')`だけでパースできることを優先

```
N|id|parentId|text                     ノード（parentIdが - ならメインテーマ＝根）
L|id|nodeA|nodeB|comment               間接エッジ
G|id|memberIds(,区切り)|title|comment  グループ
T|title|body                           ノート
```

`kind`（テーマ/アイデア）フィールドは持たせない（parentIdが `-` かどうかで判別する）。  

AIがトランザクションを提案する際は、同じフィールド形状に操作プレフィックスを付けて表現する。  

```
N+|newId|parentId|text            ノード追加（新規idはnew接頭辞。コミット時に実IDへ変換）
N-|id                             ノード削除
N~|id|text                        テキスト編集
L+|newId|nodeA|nodeB|comment      間接エッジ追加
L-|id                             間接エッジ削除
G+|newId|memberIds|title|comment  グループ新規作成
M|id,id|text                      ノード統合
T+|newId|title|body               ノート作成
```

- `text`/`comment`内に`|`や改行が含まれる場合は、シリアライズ前に安全な文字へ置換する
- バックエンドのパーサーは1行単位で解釈し、壊れた行は例外を投げず読み飛ばす

### 8.2 API設計

RESTful設計に準ずる。  

- LLM連携: OpenAI SDKのクライアントを、ローカルのOllama（`base_url=http://localhost:11434/v1`）に向けて利用する
- モデル一覧取得: OllamaのOpenAI互換`/v1/models`を`client.models.list()`経由で取得する
- モデル切替: 以降のAPI呼び出しで使用するモデル名を変更するのみとし、明示的なプリロードは行わない

共通エラー形式: 全エンドポイントで `{error: {code: string, message: string}}` を返す。  

- `validation` (400): リクエスト形式の不備
- `not_found` (404): 指定したシート/リソースが存在しない
- `llm_unavailable` (503): Ollamaに接続できない
- `llm_timeout` (504): LLM推論がタイムアウトした
- `internal_error` (500): その他の予期しないエラー

エンドポイント一覧  

リクエスト/レスポンスボディの形状は曖昧さを排すため、全エンドポイントで具体例を示す。ラップするのは共通エラー形式（`error`）と`GET /models`の`data`（OpenAI互換形式の踏襲）のみとし、それ以外は極力フラットな構造に統一する。8.1.1のシート本体（`nodes`/`links`/`groups`/`notes`）はAPI境界でも同じ形状のまま、`body`等のキーで包まずメタデータと同階層に展開する。  

- `GET /models`: 利用可能なモデル一覧を返す
  - 成功時(200): `{"data": [{"id": "qwen3.5:0.8b"}, ...]}`（OllamaのOpenAI互換`/v1/models`の応答形式をそのまま透過する）
  - 失敗時: 共通エラー形式（`llm_unavailable`）
- `GET /sheets`: シート一覧を返す
  - 成功時(200): `[{"id": "...", "title": "...", "updated_at": "..."}, ...]`（bare配列。ラップしない）
- `POST /sheet`: 新規シートを作成しIDを払い出す
  - `req: {"title": "無題"}`
  - 成功時(201): `{"id": "..."}`
- `GET /sheet/{id}`: シートを読み込む
  - 呼び出し時に`app_state.last_opened_sheet_id`を更新する
  - 成功時(200): `{"id": "...", "title": "...", "created_at": "...", "updated_at": "...", "nodes": [...], "links": [...], "groups": [...], "notes": [...]}`（メタデータとシート本体を同階層にフラット展開する）
  - 失敗時: 共通エラー形式（`not_found`）
- `PUT /sheet/{id}`: シートを保存する（部分更新ではなく全体上書き）
  - `req: {"title": "...", "nodes": [...], "links": [...], "groups": [...], "notes": [...]}`（`GET /sheet/{id}`のレスポンスから`id`/`created_at`/`updated_at`を除いた形）
  - 成功時(200): `{"ok": true}`
  - 失敗時: 共通エラー形式（`validation`/`not_found`）
- `DELETE /sheet/{id}`: シートを削除する（初期実装では物理削除。論理削除は将来拡張）
  - 成功時(200): `{"ok": true}`
  - 失敗時: 共通エラー形式（`not_found`）
- `GET /state`: 保存されている全キーの値をまとめて返す
  - 成功時(200): `{"last_used_model": "...", "user_prompt": "...", "last_opened_sheet_id": "..."}`（キーバリューを直接返す。ラップしない）
- `PUT /state`: 渡されたキーのみ部分更新する（マージ更新。存在しないキーは新規追加）
  - `req: {"last_used_model": "..."}`（更新したいキーのみを直接送る。ラップしない）
  - 成功時(200): `{"ok": true}`
  - 失敗時: 共通エラー形式（`validation`）
- `POST /ai`: 指定したシートに対してAIにトランザクションの提案を行わせる
  - `req: {"model_name": "...", "mode": "...", "sheet_id": "...", "target_node_id": "...", "system_prompt": "..."}`
    - `target_node_id`は空文字列`""`を許容する。空文字列の場合はシート全体を対象とする（4.5参照）。サーバー側は`None`判定のみで必須チェックを行い、空文字列を未指定として扱わない
  - 呼び出し前提: フロントは呼び出し直前に `PUT /sheet/{id}` で自動保存し、未保存の変更を確実に反映させる
  - 成功時(200): `{"title": "...", "ops": [...], "ghosts": [...], "removes": [...], "links": [...], "group": {...}, "note": {...}, "merge": false}`（4.3のトランザクション提案と同じ形状）
  - 失敗時: 共通エラー形式

### 8.3 DB設計

#### 物理設計

`/storage` ディレクトリ配下に実データを格納する。  

- `/storage/db.sqlite`: sqliteデータベース
  - シートのメタデータ管理、状態管理に用いる
- `/storage/sheets` ディレクトリ
  - シートのボディを1シート1ファイルで管理
  - json形式で、ファイル名はUUIDで管理

#### 論理設計

テーブル一覧  

- `sheets`: シートのメタデータ
  - `id`, `title`, `created_at`, `updated_at`
- `app_state`: キーバリュー形式の状態管理
  - `key`（主キー）, `value`
  - 想定するキー例: `last_opened_sheet_id`（最後に開いたシート）, `last_used_model`（最後に使用したLLMモデル）, `user_prompt`（ユーザー設定のデフォルトシステムプロンプト）
  - キーの追加・変更にスキーマ変更を要さない

### 8.4 非機能要件・エラーハンドリング方針

- シート保存が失敗した場合、Undo/Redo履歴と編集中の内容は保持する（変更を失わない）
- 設定（モデル・システムプロンプト）の保存リクエストは直列実行し、古いリクエストの応答が新しい入力を上書きしないようにする
- バックエンドのAPI応答が想定した形式と異なる場合は、例外を投げずエラーメッセージを画面に表示する（アプリをクラッシュさせない）
